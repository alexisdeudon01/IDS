# Variables d'environnement pour le pipeline IDS2 SOC

# Identifiants du maître OpenSearch (à remplacer par vos propres valeurs)
OPENSEARCH_MASTER_USER=admin
OPENSEARCH_MASTER_PASS=Admin123!

# Identifiants Grafana (à remplacer par des valeurs sécurisées en production)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Informations utilisateur Git pour les commits automatiques
GIT_AUTHOR_NAME="IDS2 Agent"
GIT_AUTHOR_EMAIL="ids2@localhost"
GIT_COMMITTER_NAME="IDS2 Agent"
GIT_COMMITTER_EMAIL="ids2@localhost"

# ARN de l'utilisateur IAM pour le mappage FGAC (à remplacer par votre ARN)
IAM_USER_ARN="arn:aws:iam::211125764416:user/alexis"

# Profil et région AWS par défaut
AWS_PROFILE=moi33
AWS_REGION=us-east-1
